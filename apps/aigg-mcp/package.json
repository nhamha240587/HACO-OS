{
  "name": "aigg-mcp",
  "version": "0.1.0",
  "description": "Thin MCP bridge — chuyển tool-call từ tool lập trình AI (Claude Code, Codex, Cursor, Gemini CLI) về Node-RED để xử lý nghiệp vụ AI Governance Gateway.",
  "type": "module",
  "bin": {
    "aigg-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "license": "AGPL-3.0-only"
}
